<div class="contributor-small col-24 col-sm-12 col-md-8 col-lg-6 with-fade-in mb-3 mb-sm-5">
    <div class="contributor-small-inner p-4">
        <a class="contributor-link" href="{{ site.baseurl }}/feministtech/contributors#{{ item.slug }}">
            <div class="contributor-image with-shadow">
                <img class="img-responsive lazyload" data-src="{{ site.baseurl }}{{ item.img }}" />
            </div>
            <h3 class="contributor-title mb-0 font-basteleur text-center mt-3">{{ item.firstname }} {{ item.lastname }}</h3>
        </a>
    </div>
</div>